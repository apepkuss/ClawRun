apiVersion: v1
kind: Service
metadata:
  name: ollama-cpu-ui-svc
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: ollama-cpu-ui
  ports:
    - protocol: TCP
      name: web
      port: 3000
      targetPort: 3000
