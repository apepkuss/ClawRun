apiVersion: v1
kind: Service
metadata:
  name: ollamarun-ui-svc
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: ollamarun-ui
  ports:
    - protocol: TCP
      name: web
      port: 3000
      targetPort: 3000
