apiVersion: v1
kind: Service
metadata:
  name: litellm-svc
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: litellm
  ports:
    - protocol: TCP
      name: proxy
      port: 4000
      targetPort: 4000
